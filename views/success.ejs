<!DOCTYPE html>
<html lang="en">
<%- include('./partials/head.ejs')%>
<body>
    <%- include('./partials/nav.ejs')%>
    <div class="text-center mx-auto max-w-4xl mt-20">
        <p class="text-md <%= action === 'delete' ? 'text-red-500' : 'text-green-600' %>"> The Following Entity was <%= msg %> : </p>
        <div class="text-sm mt-8">
            <p>ID: <%= student._id %></p>
            <p>Name: <%= student.name %></p>
            <p>Class: <%= student.cls %></p>
            <p>Registration Number: <%= student.registrationNumber %></p>
        </div>
    </div>

    <% if(action !== 'delete') { %>
        <div class="max-w-4xl flex justify-center my-8 space-x-4 mx-auto">
            <form action="/edit" method="GET" class="">
                <button class="px-2 py-1 font-bold text-white bg-indigo-500" type="submit">Edit</button>
                <input type="hidden" name="studentId" value="<%= student._id %>">
            </form>
            <form action="/delete" method="POST" class="">
                <button class="px-2 py-1 font-bold text-white bg-red-500" type="submit">Delete</button>
                <input type="hidden" name="studentId" value="<%= student._id %>">
            </form>
        </div>
    <% } %>
</body>
</html>